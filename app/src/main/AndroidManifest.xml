<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.francesco.mirroryoutube">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeActivity" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".OpzioniActivity" />
        <activity android:name=".TendenzeActivity" />
        <activity android:name=".IscrizioniActivity" />
        <activity android:name=".AttivitaActivity" />
        <activity android:name=".RaccoltaActivity" />
        <activity android:name=".ItemListActivity"></activity>
    </application>

</manifest>